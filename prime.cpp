#include <iostream.h>
using namespace std;
int main()
{
	int MaxN;
	cin >> MaxN;
	cout << "MaxN = " << MaxN << endl;
    bool isNotprime[MaxN/2] = {0};
    int iMax = (int)(sqrt(MaxN)/2);
    for (int i = 0; i < count; ++i)
    {
    	
    }
	return 0;
}